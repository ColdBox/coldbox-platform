<?xml version="1.0"?>
<!-- ====================================================================== 
     WireBox Standalone Build (www.coldbox.org)                                                                
     ====================================================================== -->
<project name="wirebox-build" default="build" basedir="../">
	<description>
    	Build a new distribution of WireBox Standalone
    </description>
	
	<!-- UPDATE ON EACH VERSION -->
	<property name="wirebox.version" value="2.0.0" />
	<property name="wirebox.slug"	 value="wirebox"/>
	
	<!-- Build Labels -->
	<tstamp prefix="start"/>
	<!-- Load Contrib Tasks -->
	<path id="cp">
		<fileset dir="build/lib">
			<include name="**/*.jar" />
		</fileset>
	</path>
	<!-- Define Tasks -->
	<taskdef resource="net/sf/antcontrib/antlib.xml" classpathref="cp" />

	<!-- Init -->
	<target name="init" description="Init">
		<!-- Default environment check, if not passed via -Denvironment -->
		<condition property="environment" value="local">
			<not><isset property="environment" /></not>
		</condition>
		<if>
			<available file="build/wirebox-${environment}.properties" />
			<then>
				<!-- Load env properties -->
				<echo>Loading properties from environment: ${environment}</echo>
				<loadproperties srcFile="build/wirebox-${environment}.properties"/>
			</then>
		</if>
		<!-- Load root properties -->
		<echo>Loading base properties</echo>
		<loadproperties srcFile="build/wirebox.properties"/>
	
		<!-- Build Number -->
		<propertyfile file="build/build.number" comment="Build Number for ANT. Edit not!">
			<entry key="build.number" 
					type="int" 
			     	operation="+"
					pattern="00000"
			     	default="1" />
		</propertyfile>
		<property file="build/build.number"/>
		<!-- Build Label -->
		<property name="build.label" value="wirebox-${wirebox.version}.${build.number}-${start.DSTAMP}${start.TSTAMP}"/>
		<!-- Cleanup + Init -->
		<delete dir="${dir.build}" />
		<mkdir dir="${dir.build}/wirebox/apidocs"/>
		<chmod file="${dir.build}/**" perm="g+wxrs" type="both" />
		<!-- If stg, change perms and owners -->
		<if>
			<equals arg1="${environment}" arg2="auto" />
			<then>
				<chmod file="${dir.build}/**" perm="o+wrs" type="both" verbose="true" />
				<chown owner="stg-ortus" verbose="true">
					<fileset dir="${dir.build}" />
				</chown>
			</then>
		</if>
	</target>

	<!-- Build test reports -->
    <target name="run-tests">
        <subant target="run-junit">
          	<fileset dir="tests/automation" includes="test-wirebox.xml"/>
    		<property name="environment" value="${environment}" />
        </subant>
    </target>
	
	<!--build-->
	<target name="build" description="Build a new WireBox distribution" depends="init,run-tests">
		
		<!-- Copy build ID -->
		<concat destfile="${dir.build}/wirebox/${build.label}">Built on ${start.TODAY}</concat>
		
		<!-- Copy src to WireBox namespace-->	
      	<copy todir="${dir.build}/wirebox/system/ioc">
        	<fileset dir="system/ioc" />
        </copy>
		<copy todir="${dir.build}/wirebox/system/aop">
			 <fileset dir="system/aop" />
		</copy>
		<copy todir="${dir.build}/wirebox/system/cache">
        	<fileset dir="system/cache" />
        </copy>
		<copy todir="${dir.build}/wirebox/system/core">
        	<fileset dir="system/core">
        		<include name="collections/"/>
        		<include name="conversion/"/>
        		<include name="util/"/>
        		<include name="dynamic/"/>
        		<include name="events/"/>
        	</fileset>
        </copy>
		<copy todir="${dir.build}/wirebox/system/logging">
        	<fileset dir="system/logging" />
        </copy>

        <!--Copy text files to root folder-->
		<copy todir="${dir.build}/wirebox">
			<fileset file="license.txt" />
			<fileset file="system/ioc/readme.txt" />						
		</copy>
		
		<!--Copy box.json to root -->
		<copy toFile="${dir.build}/box.json" file="box-wirebox-cachebox.json" />

		<!-- Replace Build Numbers -->
		<replaceregexp match='@build.number@' replace="${build.number}" flags="ig" byline="true">
		  <fileset dir="${dir.build}">
		  </fileset>
		</replaceregexp>
		
		<!-- Refactor cachebox namespace -->
		<echo>Refactoring for coldbox absolute paths</echo>
		<replace dir="${dir.build}" value="/wirebox/system/" summary="yes">
		  	<include name="**/*.cfc" />
			<include name="**/*.cfm" />
			<include name="**/*.xml" />
			<include name="**/*.css" />
			<replacetoken>/coldbox/system/</replacetoken>
		</replace>
		<echo>Refactoring for coldbox instantitation and cfc paths</echo>
		<replace dir="${dir.build}" value="wirebox.system." summary="yes">
		  	<include name="**/*.cfc" />
			<include name="**/*.cfm" />
			<include name="**/*.xml" />
			<include name="**/*.css" />
			<replacetoken>coldbox.system.</replacetoken>
		</replace>

		<!-- Execute ColdDoc -->
		<get dest="${dir.build}/colddoc.html" src="${url.api}${wirebox.version}&amp;path=${dir.api}&amp;wirebox_root=${dir.build}/wirebox" verbose="true"/>
		<delete file="${dir.build}/colddoc.html" />
		
		<!-- Zip API Docs -->
		<zip destfile="${dir.exports}/WireBoxDocs-${wirebox.version}.zip" basedir="${dir.build}/wirebox/apidocs"></zip>
		<!-- Zip Bundle -->
		<zip destfile="${dir.exports}/wirebox+cachebox_${wirebox.version}.zip" basedir="${dir.build}"></zip>
		<!-- Build Checksum -->
		<checksum forceoverwrite="true" fileext=".md5">
			<fileset dir="${dir.exports}">
				<include name="*.zip" />
			</fileset>
		</checksum>
		
		<!-- No CacheBox NOW -->
		<delete dir="${dir.build}/wirebox/system/cache" />
		<!--Copy box.json to root -->
		<copy toFile="${dir.build}/box.json" file="box-wirebox.json" />

		<!-- Execute ColdDoc -->
		<get dest="${dir.build}/colddoc.html" src="${url.api}${wirebox.version}&amp;path=${dir.api}&amp;wirebox_root=${dir.build}/wirebox" verbose="true"/>
		<delete file="${dir.build}/colddoc.html" />
		
		<!-- Zip Bundle -->
		<zip destfile="${dir.exports}/wirebox_${wirebox.version}.zip" basedir="${dir.build}"></zip>
		<checksum forceoverwrite="true" fileext=".md5" file="${dir.exports}/wirebox_${wirebox.version}.zip" />
		
		<!-- Perms for automated builds -->
		<chmod file="${dir.build}/**" perm="g+wxrs" type="both" />
		<!-- Move BE to root -->
		<if>
			<equals arg1="${be.name}" arg2="" />
			<then></then>
			<else>
				<copy file="${dir.exports}/wirebox_${wirebox.version}.zip" tofile="${be.exports}/${be.name}.zip" />
				<copy file="${dir.exports}/wirebox_${wirebox.version}.zip.md5" tofile="${be.exports}/${be.name}.zip.md5" />
				<copy file="${dir.exports}/wirebox+cachebox_${wirebox.version}.zip" tofile="${be.exports}/${be.name}+cachebox.zip" />
				<copy file="${dir.exports}/wirebox+cachebox_${wirebox.version}.zip.md5" tofile="${be.exports}/${be.name}+cachebox.zip.md5" />
			</else>
		</if>
		
		<!-- Cleanup -->
		<delete dir="${dir.build}" />
    </target>
</project>
