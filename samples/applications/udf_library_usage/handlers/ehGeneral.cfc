<!-----------------------------------------------------------------------Author 	 :	Your NameDate     :	September 25, 2005Description :	coldboxSamples.system.eventhandler-----------------------------------------------------------------------><cfcomponent name="ehGeneral" extends="coldbox.system.eventhandler" output="false">	<!--- ************************************************************* --->	<cffunction name="dspHome" access="public" returntype="void" output="false">		<cfargument name="requestContext" type="coldbox.system.beans.requestContext">		<cfscript>			var rc = requestContext.getCollection();			//Exit Handlers			rc.xehUDFValidation = "ehGeneral.doUseUDF";			//or use setValue("xehUDFValidation", "ehGeneral.doUseUDF");			//Set the page's title			requestContext.setValue("title", "ColdBox - Sample UDF Library Usage.");			//Set the view to display			requestContext.setView("vwHome");		</cfscript>	</cffunction>		<!--- ************************************************************* --->		<cffunction name="doUseUDF" access="public" returntype="void"  output="false">		<cfargument name="requestContext" type="coldbox.system.beans.requestContext">	<cfscript>		var rc = requestContext.getCollection();		//Use the IsValidEmail() function on the UDF.cfm that was loaded		rc.validationResults = IsValidEmail( requestContext.getValue("email") );		//Set messagebox accordingly		if ( rc.validationResults )			getPlugin("messagebox").setMessage("info","The email: #requestContext.getValue('email')# is valid.");		else			getPlugin("messagebox").setMessage("error","The email: #requestContext.getValue('email')# is not valid.");		//Run the dspHome		setNextEvent("ehGeneral.dspHome");	</cfscript>	</cffunction>	<!--- ************************************************************* ---></cfcomponent>